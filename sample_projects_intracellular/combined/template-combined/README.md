Make this project with
```sh
make reset # just to make sure everything is clean
make template-combined-intras # load the project
make -j 4 # compile the project
./project -n ./config/sample_combined_sbmls.xml # run the project
```